{
  "name": "oncomente-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "lerna": "^9.0.7",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start:backend": "lerna run start:dev --scope backend",
    "start:mobile": "lerna run start --scope mobile",
    "start:web": "lerna run dev --scope web",
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "dependencies": {
    "zustand": "^5.0.12"
  },
  "optionalDependencies": {
    "expo-router": "^6.0.14"
  }
}
